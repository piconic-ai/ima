{
  "name": "@ima/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @ima/web build && wrangler dev",
    "deploy": "pnpm --filter @ima/web build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc -p .",
    "test": "vitest run",
    "preview": "pnpm --filter @ima/web build && wrangler preview"
  },
  "dependencies": {
    "hono": "^4.13.9"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "wrangler": "^4.138.0"
  }
}
